# Runbook

## Environment
- Use the existing conda env if present (e.g., `conda activate news`).

## Common commands (edit for your stack)
- Install deps: (fill if needed)
- Run app: (fill if needed)
- Run tests:
  - If python: `pytest -q`
  - If node: `npm test`
- Lint/format:
  - Python: `ruff check .` or `flake8` (whichever exists)
  - Node: `npm run lint`

## Notes
- Do not print `.env` contents.